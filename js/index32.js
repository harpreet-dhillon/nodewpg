let length = 10;
let width = 4;
let perimeter = 2 * (length + width);
let area = length * width;
console.log(`lenght is ${length} and width is ${width}. Perimeter of rectangle is: ${perimeter} and Area of rectangle is: ${area}`);