let firstSide = 10;
let secondSide = 20;
let base = 5;
let perimeter = firstSide + secondSide + base;
console.log(`${perimeter} is the perimeter of a triangle with sides: ${firstSide}, ${secondSide}, and ${base}`);